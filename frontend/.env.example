# true（默认开发行为）使用内置 mock；false 使用 Vite 代理连接本地服务。
VITE_USE_MOCK=true

# Vite 开发代理目标。生产环境使用同源 /auth 和 /api，无需设置。
VITE_ICTHUB_API_TARGET=http://127.0.0.1:8003
VITE_FEIYUE_AUTH_TARGET=http://127.0.0.1:8001

# 仅跨域直连部署需要；通常保持为空并使用同源反向代理。
# VITE_API_BASE=http://127.0.0.1:8003
